function mockSave(message) {
    alert(message + "\n\nThis is a storyboard prototype. No database has been linked or added!");
}

function mockDelete(item) {
    confirm("Storyboard mockup: Delete " + item + "?");
}